
  # Calibre-Web
  services.calibre-web = {
    enable = true;
    user = "fabian";
    listen = {
      port = 8025;
      ip = ip_address;
    };
    options = {
      enableBookUploading = true;
    };
  };

